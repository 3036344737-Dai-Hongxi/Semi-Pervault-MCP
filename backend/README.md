# Backend Notes

VoiceVR / the old `/voice-v2` RTC-VoiceChat backend route is fully paused and parked as an unused reference.

Do not continue `StartVoiceChat`, `UpdateVoiceChat`, `StopVoiceChat`, provider callback receiver, BusinessId, or cloud media processing debugging for that route unless the roadmap is explicitly reopened.

See `../docs/plan/voicecall-status.md` for the current voice status.
